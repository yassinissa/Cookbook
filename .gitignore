# Python
backend/venv/
backend/**/__pycache__/
backend/*.pyc
backend/db.sqlite3
backend/db.sqlite3.bak.*
backend/staticfiles/
backend/media/
backend/.env

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/dev-dist/
frontend/.env

# Source data workbooks — imported via `manage.py import_*`, kept out of git
# (large binaries; the imported DB rows are the artefact that matters)
/*.xlsm
/*.xlsx
/*.xls

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code local settings (not shared)
.claude/settings.local.json

# Scratch: full skill-repo clones used while installing skills — the skills
# themselves live in .claude/skills/. Safe to delete.
_to_delete/
