SECRET_KEY=change-me
ACCESS_TOKEN_LIFETIME=60
REFRESH_TOKEN_LIFETIME=10080

# Postgres (production only — development uses sqlite)
DB_NAME=cookbook
DB_USER=cookbook
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

ALLOWED_HOSTS=
CORS_ALLOWED_ORIGINS=

# Inventory-platform integration
INVENTORY_API_BASE_URL=http://localhost:8000/api
INVENTORY_API_EMAIL=
INVENTORY_API_PASSWORD=

# Email — weekly cost digest (dev uses the console backend regardless)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=Cookbook <cookbook@greenhills.local>
FRONTEND_URL=http://localhost:5180
