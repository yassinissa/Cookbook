# Recipe/prep/dish content models go here — deliberately left empty until
# the source cookbook spreadsheet defines what fields they need (steps,
# photos, allergens, etc). Ingredient quantities, units, and costs are not
# duplicated here — they live in the inventory platform's ProductionRecipe /
# DishRecipe and are pushed there via apps.integrations.inventory_client.
